# Local Dev Setup
1. Install node.js
2. Install Hugo 0.75.1 (https://github.com/gohugoio/hugo/releases/tag/v0.75.1)
3. Run `npm install`
4. Start the site via Ctrl+Shift+B in vscode (`npx tinacms dev -c "hugo server -D -p 1313"`)
